name: Generate ASCII Artwork

on:
    push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
        
        - name: Checkout Repo Files
          run: ls -ltra

        - name: Executions shell script
          run: |
            chmod +x ASCII-script.sh
            ./ASCII-script.sh

        
