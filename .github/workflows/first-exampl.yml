name: My First Workflow
 
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "Welcome to github actions job"

        - name: List the files
          run: ls

        - name: Read the file
          run: cat README.md
